# v0.11.20 发布验收

日期：2026-09-24。

## 内容

发布当前工作区的 build/dev 性能优化、持久 Markdown 缓存、解析缓存、模板与输出优化，以及脚注和 zhurongshuo 主题更新。保留 VERSION 0.11.20。本地 backup/ 不入库。

## 发布前验证

- 主模块 `go test ./...` 通过。
- 所有独立插件模块 `go test ./...` 通过。
- `python3 -m unittest discover -s scripts -p '*_test.py'`：3 项通过。
- Admin UI `npm run build` 通过（存在非阻断的 bundle 大小提示）。
- Go 1.27.1 darwin/arm64、CGO_ENABLED=1、相同默认编译参数构建主程序与全部 8 个插件；插件完成 macOS segment 修补与签名。
- 新二进制 plugin list 成功加载 zhurongshuo 配置的 5 个插件。
- HUAN_STRICT_I18N=true 实际站点生产构建通过：zh-cn 1532 页、en 1478 页。验收输出位于 release/v0.11.20/site-check，不覆盖站点 docs。

## 发布方式

提交源码并推送 v0.11.20 标签，由 GitHub Actions 生成跨平台 Release 与包含全部插件的 GHCR 镜像。本地安装至 Go GOBIN，插件同步至 GOBIN/plugins 和 ~/.huan/plugins；站点 CI 镜像升级到同版本。远端发布结果以 GitHub Release/Actions 状态为准。
