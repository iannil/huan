# huan E2E 全量逐例实跑报告（2026-08-15）

> run_id: ${run_id} · 二进制 commit: `98172e8` + worktree dist · 环境: macOS darwin/arm64 go1.26.2
> **口径：本次为全量逐例实跑**——`go test ./...` 32 包 + E2E 全部 117 例（API 79 + CLI 15 + browser 23 旅程），非抽样。

## 1. Go 单元/集成测试

`go test ./...`：**32 包全绿，0 FAIL**。

## 2. CLI 套件（15 例全过）

build 8（cb-001~008）+ new 3（cn-001~003）+ config 3（cc-001~003）+ version 1（cv-001）——全部 PASS。含 -D 单页落盘、-F/-E 三态矩阵、缺配置非零退出、`${VAR:-default}` 插值。

## 3. API 套件（79 例全过）

| 套件 | 结果 |
|---|---|
| auth (6) | ✅ 6/6（Bearer/X-header 等价、401 双错误体+WWW-Authenticate、loopback 自动 token、非 loopback 拒启 exit 1） |
| status (4) | ✅ 4/4（初始全字段、创建后 total/published+1、recentContent 降序、mediaCount+1） |
| content-crud (14) | ✅ 14/14（创建/落盘/回读/PUT/DELETE/重复 500 不覆盖/非法 JSON/不存在/特殊字符 title 往返/.md 补全/并发 PUT×5 最后写赢+审计/draft 联动/审计 SHA/languages 多语） |
| media (8) | ✅ 8/8（SVG 上传落盘、列表、删除、删不存在 500、.exe 拒、缺 file 拒、PNG 魔数 `89504e470d0a1a0a`、dir 子目录） |
| settings (9) | ✅ 9/9（结构化/YAML 双通道、非法 YAML/JSON 400、交叉一致、异步 rebuild 首页 title、审计 settings.update+双 SHA） |
| build-trigger (7) | ✅ 7/7（202、新页可见、增量内容可见性、**draft 单页 404 + sitemap 零泄露**、模板变更重渲染、--disableWatch+POST build 按需、sitemap 含新页） |
| public-api (12) | ✅ 12/12（分页结构、limit clamp 50、越界空页、**page=MaxInt64 clamp 100000** DoS 回归、section/tag/q 过滤、201/200 rune 边界、单页详情、404/405） |
| sse-events (5) | ✅ 5/5（text/event-stream 三头、20s `:heartbeat`、`content_changed{trigger:admin}`、直改文件→增量 build_completed、插件 load/unload 广播） |
| plugins-admin (10) | ✅ 10/10（list total=3 compiled+loaded、load 400/500/200、unload 回读+404 反证、reload 三态 409 冲突、compiled 卸载拒、theme 空态 400/200、**daemon serve 页注入 og:×4**） |
| livereload (4) | ✅ 4/4（WS 101 握手+固定 Accept、hello 帧 official-7、**fs 改文→reload 帧广播**、livereload.js v4.0.2） |

## 4. Browser 旅程（23 例：22 PASS + 1 PARTIAL）

| 套件 | 结果 |
|---|---|
| admin-login (4) | ✅ lg-001 见面板、lg-002 错 token 拒、lg-003 刷新保持会话、lg-004 会话失效重认证（含 sessionStorage 实测） |
| admin-content-crud (7) | ✅ bc-001 列表+统计、bc-002 新建（.zh-CN.md 落盘）、bc-003 编辑+Ctrl+S 保存、bc-004 删除+确认弹窗+404、bc-005 草稿开关、bc-006 多语言 siblings 切换、bc-007 **en 文章 filePath 删除 200**（Task 2 修复验证） |
| admin-settings (4) | **bs-002 PARTIAL**（见下）；bs-001 回显、bs-003 YAML 通道、bs-004 非法值拒 |
| admin-plugins (3) | ✅ bp-001 列表 total=2、bp-002 加载 simple-plugin.so→total=3、bp-003 重载+卸载→total=2 |
| site-rendering (5) | ✅ sr-001 首页 **2 条链接不含草稿**（修复验证）、sr-002 文章页、sr-003 404、sr-004 tag 页、sr-005 **双 tab LiveReload 自动刷新** |

### bs-002 PARTIAL：改标题保存后「全站标题联动」未完全达成

- 已达成：保存按钮禁用→可点（fix Task 3 useState 生效）、settings API 回读新标题、huan.yaml 落盘新标题、paginate=10 未零值覆盖
- 未达成：**Dashboard 顶栏/概览仍显示旧站名**「huan E2E 最小站」
- 根因（源码定位）：`internal/admin/api.go:38` `status.title = h.siteTitle`，`siteTitle` 是 handler 构造时的静态副本（`handler.go:55` `opts.Cfg.Title`），settings PUT 触发的 async rebuild **不刷新这个缓存** → status.title 与 settings.title 成两源
- 建议：status 直接读 settings 当前值，或 rebuild 后同步刷新 handler 的 siteTitle
- **这是真实 UI bug（P2，表头/概览显示陈旧站名），非测试执行问题**

## 5. 全量实跑新发现（本报告独有，前序基线未见）

| # | 发现 | 严重度 | 性质 |
|---|---|---|---|
| 1 | dev 产物路径无 JIT、settings PUT 不刷新 status.title（bs-002 PARTIAL） | P2 | 真实 UI bug |
| 2 | 多语言编辑器顶栏文件路径显示 `.zh-CN.zh-CN.md` / `.en.en.md` 双后缀 | P2 | 显示 bug（保存走 relPath 安全，仅路径文本错）——`realFilePath` 在 relPath 已含语言后缀时重复叠加 |
| 3 | 内嵌 SPA bundle 时效：E2E 需重建 `web/admin` dist（bc-007 初测踩旧 bundle） | 测试 | 执行契约（非引擎 bug） |

## 6. 结论

- **全量逐例实跑：115/117 PASS，1 PARTIAL（bs-002），0 FAIL**
- Go 32 包全绿；P0 用例（API 52 + CLI 8 + browser P0 5）全过
- 3 个新发现（2 个真实 UI bug + 1 个执行契约）建议另立修复任务并同步 FIXTURES.md

*本报告为全量实跑基线，可与 `2026-08-15-system-baseline.md`（抽样口径）对照补全覆盖。*
