# v0.8.0 文档同步 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将项目文档同步到 v0.8.0 实际状态——补齐 2026-08-31 四个构建特性的完成报告，更新 CURRENT_STATE.md / INDEX.md 的版本与过时信息。

**Architecture:** 纯文档修改，不改代码。三份文档按「先报告、后状态、再导航」顺序更新（报告是 INDEX/CURRENT_STATE 引用的目标，先建被引用方）。最后追加每日记忆。

**Tech Stack:** Markdown 文档；无代码变更、无测试变更。

## Global Constraints

- 语言：全部中文（CLAUDE.md 约定：交流与文档使用中文）
- 文档位置：完成报告放 `docs/reports/completed/`；状态文档 `docs/progress/CURRENT_STATE.md`；导航 `docs/INDEX.md`
- 事实基准：以 git log（v0.8.0 = c9d3f26，2026-09-01 00:17）与既有报告为准，**不虚构**未发生的验证
- 关键事实澄清：**bs-002（Dashboard 站名缓存）与 `.zh-CN.zh-CN.md` 双后缀两个 P2 bug 已在 commit 25c801b 修复**（2026-08-15），`docs/reports/e2e/2026-08-15-full-run.md` 已更新为 **117/117 PASS** 并经 agent-browser 重走确认。CURRENT_STATE.md §十 中仍列为「全量实跑新发现」是过时信息，必须修正
- 版本号事实：v0.7.2（eb99142，practice/list 过滤）→ v0.8.0（c9d3f26，qwen3 移除 + 4 个构建特性）。`internal/version/VERSION` 当前为 `0.8.0`
- 本计划不改代码、不跑 `go test`（无代码变更）；验证手段为 grep 核对内容与相对链接

---

### Task 1: 补写 2026-08-31 构建特性批次完成报告

**Files:**
- Create: `docs/reports/completed/2026-08-31-build-hardening.md`

**Interfaces:**
- Consumes: git 提交记录（033fdcc / a1ae5c5 / 6015558 / 6bfd2d5 / df2072a / eb99142 / dc28459 / c9d3f26）、`huan.example.yaml` 中新配置项、既有报告 `2026-08-31-deprecate-qwen3-translate.md`
- Produces: 后续 Task 2 / Task 3 引用的报告路径 `docs/reports/completed/2026-08-31-build-hardening.md`

**背景（执行者必读）：** 2026-08-31 晚至 09-01 凌晨，master 上连续落地 6 个 commit 形成两个版本。除 qwen3 移除（已有报告 `2026-08-31-deprecate-qwen3-translate.md` + ADR 0015）外，还有 4 个构建特性和 2 个修复**没有任何 completed 报告**，只有 `memory/daily/2026-08-31.md` 的散记。本任务补齐这部分。各 commit 的改动范围（`git show --stat` 实测）：

| commit | 主题 | 改动文件 |
|---|---|---|
| 033fdcc | CopyStatic 支持前缀排除 | `internal/output/writer.go` +12、`writer_test.go` +40、`internal/build/pipeline_write.go` |
| a1ae5c5 | 构建前清理 publish 目录（cleanPublishDir，默认开） | `internal/config/config.go` +9、`internal/build/pipeline_setup.go` +18、`pipeline_setup_test.go` +36、`multisite.go` |
| 6015558 | 多语言构建默认语言始终先构建 | `internal/build/multisite.go`、`multisite_test.go` +58 |
| 6bfd2d5 | 每语言独立 staticExclude + staticRoot | `internal/config/languages.go` +11、`internal/build/multisite.go`、`pipeline_write.go`、`multisite_test.go` +53、`huan.example.yaml` +4 |
| df2072a（v0.7.2） | practice/list 过滤 introduction 仅根级 | `plugins/zhurongshuo/templates/practice/list.html` |
| c9d3f26 | 版本号 bump 0.8.0 | `internal/version/VERSION` |

- [ ] **Step 1: 阅读报告模板与参考报告**

Read: `docs/templates/report-template.md`（结构模板）与 `docs/reports/completed/2026-08-31-deprecate-qwen3-translate.md`（同日报告，风格基准——注意其「验证」一节如实记录了 `go test ./...` 31 包全过）。

- [ ] **Step 2: 查看各 commit 细节以撰写准确的报告**

Run:
```bash
git show 033fdcc -- internal/output/writer.go
git show a1ae5c5 -- internal/config/config.go
git show 6015558 -- internal/build/multisite.go
git show 6bfd2d5 -- internal/config/languages.go internal/build/multisite.go
```
重点提取：每个配置项的名称、默认值、YAML 示例写法（`huan.example.yaml` 中的实际段落）。

- [ ] **Step 3: 撰写报告**

Write `docs/reports/completed/2026-08-31-build-hardening.md`，按 report-template 结构，内容必须覆盖：

```markdown
# 2026-08-31 构建健壮性批次（v0.7.2 → v0.8.0）完成报告

> 完成日期：2026-09-01（commit c9d3f26，tag v0.8.0）
> 关联：qwen3 移除见 [2026-08-31-deprecate-qwen3-translate.md](2026-08-31-deprecate-qwen3-translate.md) 与 ADR 0015

## 1. 概述
（一段话：v0.7.2→v0.8.0 的构建管线四特性 + zhurongshuo 模板修复；动机均为 zhurongshuo 双语生产构建中的实际问题——stale 文件残留、语言目录静态资源串扰、构建顺序不确定）

## 2. 新增依赖
无。

## 3. 新增 / 修改的包
| 路径 | 职责 | 关键文件 |
|---|---|---|
| internal/output | CopyStatic 前缀排除 | writer.go |
| internal/config | cleanPublishDir（默认 true）；LanguageConfig 增加 staticExclude/staticRoot | config.go / languages.go |
| internal/build | 默认语言先构建；publish 目录清理 | multisite.go / pipeline_setup.go / pipeline_write.go |
| plugins/zhurongshuo | practice/list 仅根级 introduction | templates/practice/list.html |

## 4. CLI / 配置变更
| 配置 | 默认值 | 说明 |
|---|---|---|
| cleanPublishDir | true | 构建前清空 publish 目录，消除 stale 产物 |
| languages.<code>.staticExclude | （空） | 每语言静态资源排除前缀列表 |
| languages.<code>.staticRoot | （空） | 每语言静态资源根目录 |
（每行附 huan.example.yaml 中的实际示例片段）

## 5. 关键设计决策
（从 commit message 与 diff 提炼；默认语言先构建的原因：后续语言构建依赖默认语言产物基础）

## 6. 验证
如实记录：各 commit 均附单元测试（writer_test / pipeline_setup_test / multisite_test 共 +149 行）；
zhurongshuo 全量构建 zh-cn=1162 / en=1109 pages、stale check 982/0/0（引自 daily note 2026-08-31 与
qwen3 报告的实测记录）。不要声称执行了新的验证。
```

- [ ] **Step 4: 核对报告内相对链接与事实**

Run:
```bash
ls docs/reports/completed/2026-08-31-deprecate-qwen3-translate.md
grep -c "staticExclude\|cleanPublishDir\|staticRoot" docs/reports/completed/2026-08-31-build-hardening.md
```
Expected: 第一条命令列出文件存在；第二条计数 ≥ 3。

- [ ] **Step 5: Commit**

```bash
git add docs/reports/completed/2026-08-31-build-hardening.md
git commit -m "docs: completed report for v0.7.2-v0.8.0 build hardening batch"
```

---

### Task 2: 更新 CURRENT_STATE.md 到 v0.8.0 并修正过时信息

**Files:**
- Modify: `docs/progress/CURRENT_STATE.md`

**Interfaces:**
- Consumes: Task 1 的报告路径；git log 事实；full-run 报告 `docs/reports/e2e/2026-08-15-full-run.md`（117/117 PASS 结论）
- Produces: 与 v0.8.0 一致的状态文档（Task 3 的 INDEX 将引用它）

**执行者必读的过时点清单（逐条修正）：**
1. 第 3 行：「当前版本：v0.7.1 …最后更新 2026-08-15」→ `v0.8.0（tag v0.8.0，2026-09-01）· 最后更新 2026-09-01`
2. §二 版本演进表：缺 v0.7.2 和 v0.8.0 两行，需在表首补：
   - `| **v0.7.2 / v0.8.0** | 2026-08-31~09-01 | **qwen3-translate 完全移除**（ADR 0015，-4214 行；site_translations 迁移 languages.<code>.params）+ 构建健壮性批次（cleanPublishDir 默认开 / 每语言 staticExclude+staticRoot / CopyStatic 前缀排除 / 默认语言先构建）。详见 3.14 与 [报告](../reports/completed/2026-08-31-build-hardening.md) |`
3. §三 追加 **3.14 构建健壮性批次（2026-08-31，v0.8.0）** 小节（4 个特性各 1-2 行 + 指向 Task 1 报告的链接），同时说明 `huan translate` 已移除（第 16 行已有提及但需确认与 ADR 0015 一致——把「见 ADR 0011」改为「见 ADR 0015」，注意第 16 行现写的是 ADR 0011，而 daily note 明确 ADR 0011~0014 已被占用、qwen3 废弃是 ADR 0015）
4. §五 CLI 子命令表：无子命令增删（translate 非子命令表内条目，确认无残留即可——用 `grep -n translate docs/progress/CURRENT_STATE.md` 检查，若 §三之外仍有「huan translate 可用」语义的描述则删除）
5. §七 无需改动
6. §九 已知限制：无需改动（bs-002 不在此节）
7. §十 两处修正：
   - 「2026-08-15 全量逐例实跑」小节：结果行 `115/117 PASS，1 PARTIAL（bs-002），0 FAIL` 已过时 → 改为 `**117/117 PASS，0 FAIL**（途中发现的 2 个 UI bug 已在当日修复并经 agent-browser 重走确认，见报告更新）`
   - 「全量实跑新发现」列表：bs-002 与双后缀两条标注「已修复」（commit 25c801b，`status.title` 改实时 readSettings + realFilePath 幂等 + TestRealFilePath_Idempotent 回归）；第三条（SPA bundle 时效）保留为执行契约
8. 文档头部「最后更新」与 §一 版本节奏行（第 17 行）补 v0.8.0：`→ v0.8.0（2026-09-01 qwen3 移除 + 构建健壮性）→ v1.0.0（gate 6 …）`

- [ ] **Step 1: 按上面清单逐条修改 CURRENT_STATE.md**

用 Edit 工具逐条修改（8 处）。§三 新增小节示例：

```markdown
### 3.14 构建健壮性批次 + qwen3-translate 移除（2026-08-31，v0.7.2 → v0.8.0）

- **qwen3-translate 完全删除**（ADR 0015）：`plugins/qwen3/`、`cmd/huan/translate_*.go`、`internal/translate/`、`pkg/translate/` 全删（-4214 行）；`site_translations` 迁移到 `languages.<code>.params`；`.en.md` sidecar 保留参与构建，翻译改站外维护
- **cleanPublishDir**（默认开）：构建前清空 publish 目录，消除 stale 产物
- **每语言 staticExclude + staticRoot**：多语言构建中各语言静态资源独立排除与根目录
- **CopyStatic 前缀排除** + **默认语言始终先构建**
- 报告：[2026-08-31-build-hardening.md](../reports/completed/2026-08-31-build-hardening.md) / [2026-08-31-deprecate-qwen3-translate.md](../reports/completed/2026-08-31-deprecate-qwen3-translate.md)
```

- [ ] **Step 2: 验证修改完整性**

Run:
```bash
grep -n "v0.7.1\|115/117\|ADR 0011" docs/progress/CURRENT_STATE.md
grep -c "3.14" docs/progress/CURRENT_STATE.md
```
Expected: 第一条——`ADR 0011` 允许出现在 §版本演进表 v0.7.0 行（历史事实，ADR 0011 是 admin-security），但 **不得**再出现「v0.7.1（当前版本）」语义或「115/117」；若第一列 grep 出的行是历史演进表中 v0.7.0/v0.7.1 行的描述则保留，是「当前版本」行则说明 Step 1 漏改。第二条计数 ≥ 2（§二 表引用 + §三 小节标题）。

- [ ] **Step 3: Commit**

```bash
git add docs/progress/CURRENT_STATE.md
git commit -m "docs: CURRENT_STATE sync to v0.8.0, mark bs-002/double-suffix fixed"
```

---

### Task 3: 更新 INDEX.md 导航

**Files:**
- Modify: `docs/INDEX.md`

**Interfaces:**
- Consumes: Task 1 报告路径、Task 2 的版本事实
- Produces: 与 v0.8.0 一致的文档导航

- [ ] **Step 1: 修改三处**

1. 第 4 行：`> **当前版本**：v0.6.0+（详见 [版本时间线](#版本时间线)）` → `> **当前版本**：v0.8.0（2026-09-01，详见 [版本时间线](#版本时间线)）`
2. 文档树表（`## 文档树`）：ADR 0015 行已存在，无需改；确认文档树中有 `progress/CURRENT_STATE.md` 行（已有）。
3. `## 版本时间线` 表（约 169 行起）表首补一行：
   `| **v0.7.2 / v0.8.0** | 2026-08-31~09-01 | **qwen3-translate 完全移除**（ADR 0015）+ **构建健壮性批次**：cleanPublishDir 默认开 / 每语言 staticExclude+staticRoot / CopyStatic 前缀排除 / 默认语言先构建 |`

- [ ] **Step 2: 验证**

Run:
```bash
grep -n "v0.6.0+" docs/INDEX.md
grep -c "v0.8.0" docs/INDEX.md
```
Expected: 第一条无「当前版本」行的命中（时间线表内历史行 `v0.6.0+` 保留——它描述的是 v0.6 起的时代段，允许存在）；第二条计数 ≥ 2。

- [ ] **Step 3: Commit**

```bash
git add docs/INDEX.md
git commit -m "docs: INDEX sync to v0.8.0"
```

---

### Task 4: 追加每日记忆与收尾核对

**Files:**
- Create: `memory/daily/2026-09-01.md`（若不存在）
- Modify: `memory/MEMORY.md`（仅当有过时条目需更正时）

**Interfaces:**
- Consumes: Task 1-3 的全部产出
- Produces: 符合 CLAUDE.md 记忆约定的当日记忆

- [ ] **Step 1: 追加当日记忆**

`memory/daily/2026-09-01.md` 若不存在则创建，格式对齐 `memory/daily/2026-08-31.md`。内容（可直接使用，按实际执行情况微调）：

```markdown
# 2026-09-01

## 文档同步到 v0.8.0

- 背景：v0.8.0（c9d3f26，09-01 00:17）发布后 CURRENT_STATE.md 停在 v0.7.1、INDEX.md 停在 v0.6.0+，
  08-31 的 4 个构建特性无 completed 报告
- 新增报告：docs/reports/completed/2026-08-31-build-hardening.md（cleanPublishDir / 每语言
  staticExclude+staticRoot / CopyStatic 前缀排除 / 默认语言先构建 / practice-list 修复）
- CURRENT_STATE.md：版本演进补 v0.7.2/v0.8.0 两行 + 新增 §3.14；修正 §十 过时信息——
  bs-002 与 .zh-CN.zh-CN.md 双后缀实际已在 08-15 commit 25c801b 修复，full-run 已是 117/117 PASS
- INDEX.md：当前版本行 + 版本时间线补行
- 重要澄清：CURRENT_STATE 之前列的「遗留 P2 bug」已不存在；真正的遗留见 CURRENT_STATE §九 已知限制
```

- [ ] **Step 2: 检查 MEMORY.md 是否有过时条目**

Run: `grep -n "translate\|qwen3\|v0.7" memory/MEMORY.md`
若有条目声称「qwen3/translate 仍可用」或版本停留在 v0.7.x 的「当前状态」语义描述，更新该条目；若只是历史教训类记录（如 Go plugin buildid 契约）则不动。

- [ ] **Step 3: 全局链接核对**

Run:
```bash
ls docs/reports/completed/2026-08-31-build-hardening.md docs/reports/completed/2026-08-31-deprecate-qwen3-translate.md
grep -rn "2026-08-31-build-hardening" docs/ | wc -l
```
Expected: 两个文件都存在；引用计数 ≥ 2（报告自身之外，CURRENT_STATE / INDEX 至少一处引用）。

- [ ] **Step 4: Commit**

```bash
git add memory/daily/2026-09-01.md memory/MEMORY.md
git commit -m "docs(memory): daily note for v0.8.0 doc sync"
```

---

## Self-Review 记录

- **Spec 覆盖**：文档同步（Task 1-3）+ 记忆约定（Task 4）全覆盖；P2 bug 修复经调查确认已完成（25c801b），计划范围收敛为文档，已在开头声明。
- **占位符扫描**：Task 1 Step 3 的报告骨架中「（从 commit message 与 diff 提炼）」是执行指令而非交付占位符，数据来源在 Step 2 给出；其余步骤均含完整内容。
- **类型一致性**：报告文件名在 Task 1/2/3/4 之间统一为 `2026-08-31-build-hardening.md`；版本号统一 v0.7.2→v0.8.0；commit 引用（c9d3f26 / 25c801b）全文一致。
