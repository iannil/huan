/huan
/equiv-check
/release/
/archived/
.DS_Store

# Admin frontend
web/admin/node_modules/

# Editor / IDE
.agents/
.claude/
skills-lock.json
# Plugin binaries — never tracked. Compiled via scripts/build-plugins.sh into
# release/plugins/ (also gitignored); resolved at runtime from $HUAN_HOME (~/.huan)
# then <project>/plugins.
*.so
