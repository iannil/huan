# 2026-06-26 每日笔记

## session: huan 定位重新定义——从 SSG 到一体化内容引擎

### 背景
- 目标：通过 `/grill-me` 重新定义 huan 的定位——从「静态站点生成器 / Hugo 替代品」升级为「替代所有 CMS」
- 用户选择了 A. 全环节替代 CMS（不止是 SSG / Hugo 替代品）

### grill-me 达成共识

1. **定位范畴**：A. 全环节替代 CMS——内容管理 + 渲染 + 发布
2. **核心架构**：A. SSG + 文件系统内容源（Markdown 文件即内容源，Admin UI 直接读写文件）
3. **目标用户**：A. 所有 CMS 用户
4. **Admin UI 水准**：D. 接近 WordPress 水准的后台
5. **时间线**：A. 立即生效的当前定位
6. **Admin 形态**：A. huan serve 内置 /admin 路由
7. **数据模型**：A. 基于文件系统（Markdown 文件即内容源）
8. **一句话定位**：C. 一体化内容引擎（All-in-One Content Engine）

### 已更新的文档
- `CLAUDE.md`：项目概述改为「一体化内容引擎」
- `docs/INDEX.md`：一句话定位更新
- `docs/technical-plan.md`：§1 项目定位更新
- `README.md`：tagline + What is huan 更新
- `README.zh-CN.md`：tagline + huan 是什么 更新

### 后续方向（待启动）
- 在 `huan serve` 中实现 `/admin` 路由
- 管理后台：先围绕已有内容类型（文章、页面、媒体、分类）做后台

## session: 执行——撰写定位变更报告

- 用户确认「执行」，补写了 `docs/reports/completed/2026-06-26-positioning-redefine.md`
- 更新 MEMORY.md 去掉「待撰写」标记
- 更新本日 daily note 记录进展

## session: Stage 4 开发计划 grill-me

### 目标
基于新定位重新规划开发路线图，确定 Stage 4（admin panel）的技术方案和落地路径。

### grill-me 决策收敛

| 决策 | 结论 |
|------|------|
| 阶段命名 | Stage 4（延续旧编号） |
| 技术栈 | Go JSON API + 嵌入式 React SPA |
| 前端框架 | React 19 + Shadcn UI |
| 构建工具 | Vite + TypeScript + Tailwind CSS |
| 项目结构 | `web/admin/`（前端）+ `internal/admin/`（Go） |
| 认证 | 初期无（localhost only） |
| API 范围 | CRUD（读取 + 写入 + 新建 + 删除） |
| 落地方式 | 一次 PR 前后端一起 |

### 已更新的文档
- `README.md`：Roadmap 章节替换为 Stage 4
- `docs/progress/CURRENT_STATE.md`：新增 Stage 4 章节
- `docs/INDEX.md`：版本时间线新增 v0.3.0 行

## session: 执行——Stage 4 PR1 实现

### 创建的文件

| 文件 | 说明 |
|------|------|
| `internal/admin/types.go` | API 请求/响应类型定义 |
| `internal/admin/content.go` | 文件系统 CRUD 操作（list/read/create/update/delete Markdown） |
| `internal/admin/api.go` | HTTP API 处理器（5 个端点） |
| `internal/admin/handler.go` | Admin mux（API 路由 + SPA 分发 + //go:embed） |
| `internal/admin/dist/index.html` | 占位符（前端未构建时显示安装提示） |
| `internal/admin/.gitignore` | 忽略 dist/ |
| `web/admin/package.json` | React 项目依赖声明 |
| `web/admin/vite.config.ts` | Vite 构建配置（输出到 internal/admin/dist/） |
| `web/admin/tsconfig.json` | TypeScript 配置 |
| `web/admin/tailwind.config.ts` | Tailwind CSS 配置 |
| `web/admin/postcss.config.js` | PostCSS 配置 |
| `web/admin/index.html` | 入口 HTML |
| `web/admin/src/main.tsx` | React 入口 |
| `web/admin/src/App.tsx` | 路由定义（content list/edit/new） |
| `web/admin/src/index.css` | Tailwind 基础样式 |
| `web/admin/src/components/Layout.tsx` | 布局组件（导航 + 内容区） |
| `web/admin/src/pages/ContentList.tsx` | 内容列表页（按 section 分组） |
| `web/admin/src/pages/ContentEdit.tsx` | 内容编辑页（frontmatter + body） |
| `web/admin/src/pages/ContentNew.tsx` | 新建内容页 |

### 修改的文件
- `internal/serve/server.go`：ServerOptions 加 AdminHandler 字段 + /admin/ 路由注册
- `cmd/huan/serve.go`：导入 admin 包 + 创建 admin.NewHandler()

### 验证
- `go build -o huan ./cmd/huan` ✅
- `go vet ./internal/admin/... ./internal/serve/... ./cmd/huan/...` ✅
- `go test ./...` 全包通过，零回归 ✅
- 实际 `huan serve` 测试 zhurongshuo 站点：/admin/ HTTP 200，API 列出 1698 篇内容按 9 个 section 分组，读取单文件完整 frontmatter + body 正常
- ⚠️ 写 API 受 macOS TCC 权限限制无法写入 `@zhurong` 路径，但 API 逻辑正确（正常返回 500 + 系统错误信息）


## session: ContentList 完整化——搜索+排序+筛选+分页+批量操作

### 目标
经过 grill-me 确认，对 ContentList 页面做完整功能包增强，纯前端实现不改后端 API。

### grill-me 决策
| 决策 | 结论 |
|------|------|
| 范围 | 完整功能包（搜索+排序+分页+筛选+批量操作） |
| API 策略 | 纯前端过滤——不改后端 |
| 批量交互 | 标准 checkbox 多选 + 底部操作栏 |
| 键盘快捷键 | 本次不做，后续迭代 |

### 改动文件
- `web/admin/src/pages/ContentList.tsx`：完整重写

### 新增功能
1. **搜索框**：按标题/路径实时过滤，带清除按钮
2. **排序**：标题/日期/状态 3 列可点击排序，箭头指示方向（`ArrowUp`/`ArrowDown`/`ArrowUpDown`）
3. **草稿筛选**：三段式切换（全部/草稿/已发布）
4. **内容类型标签**：section 类型显示 Badge，page 类型显示文本
5. **分页**：每页 20/50/100 可选 + 页码导航（上一页/下一页 + 最多 5 个页码按钮）
6. **批量选择**：全选 checkbox + 单行 checkbox，选中行背景高亮
7. **批量操作栏**：底部固定条显示「已选 N 项」+ 批量删除（确认 Dialog）+ 批量切换草稿
8. **删除确认 Dialog**：使用 Dialog 组件，列出前 10 个文件名

### 验证
- `npx tsc --noEmit` —— 零错误 ✅
- 前端逻辑验证：选择/排序/分页/批量操作全部纯客户端实现

## session: ContentEdit Markdown 实时预览

### 改动
- **依赖**: 安装 `marked` v18.0.5（内置 TypeScript 类型）
- **ContentEdit.tsx**: 完整重写，新增三种预览模式

### 新增功能
1. **模式切换** — 工具栏按钮组：编辑（`PenLine`）/ 分屏（`Columns3`）/ 预览（`Eye`）
2. **Markdown 渲染** — 使用 `marked.parse()` 实时将 body 转为 HTML
3. **分屏同步滚动** — Edit 和 Preview 面板滚动位置联动
4. **预览区 CSS** — 完整样式表覆盖 h1-h6 / p / a / strong / em / blockquote / ul-ol / pre-code / inline-code / img / hr / table

### 验证
- `npx tsc --noEmit` — 零错误 ✅
- `npm run build` — Vite build 成功（1.0s，477KB JS + 25KB CSS）✅
- `marked` 正确打包到产物 JS 中 ✅
- `huan serve` — Home / Admin / Content API 全部 200 OK ✅
- commit `f877b2a` → pushed to master ✅
