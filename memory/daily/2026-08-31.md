# 2026-08-31

## 废弃 qwen3-translate 插件（ADR 0015）

用户决定完全删除 qwen3-translate 插件及全套 Translator 翻译基建（非 deprecation 保留）。

### 决策要点（AskUserQuestion 确认）

1. 废弃方式：**完全删除**
2. 翻译基建（Translator capability / huan translate 命令 / pkg/translate）：**一并删除**
3. zhurongshuo 站点：**同步清理**（.en.md sidecar 保留继续参与构建）

### 执行

- huan 仓库：删除 `plugins/qwen3/`（17 文件独立 module）、`cmd/huan/translate_*.go`（6 文件）、`internal/translate/`、`pkg/translate/`、`internal/build/inject_translations.go`；daemon/plugins.go 移除 translate capability 探测
- **site_translations 迁移**：`plugins.qwen3_translate.site_translations` → `languages.<code>.params`（新 `LanguageConfig.Params` + `multisite.go::applyLanguageParams`，部分覆盖语义与原行为一致）
- zhurongshuo：huan.yaml 删 qwen3_translate 块、deploy.sh 10→8 步（删 step5 翻译 / step8 backfill，删 --skip-translate）、删 `i18n/translate-prompt-zh-en.md`、删 `~/.huan/plugins/qwen3-translate.so`
- 清理当天 qwen3 运行残留：`content/hidden/kachuai/dao/1.en.md`（hidden section 被 en 构建排除，此文件导致 hreflang 指向 404）

### 验证

- `go build` / `go vet` / `go test ./...` 31 包全过
- zhurongshuo 实际构建 zh-cn=1162 / en=1109 pages，stale check 982/0/0
- 英文站 subTitle 注入正确、无中文 params 泄漏、构建产物与 git HEAD byte 级一致

### 注意

- ADR 0011~0014 编号已被占用（admin-security / theme-plugin / contract-convergence / hook-split），本决策为 **ADR 0015**
- `.en.md` sidecar 的人工维护：新增/更新翻译需自行维护 source_hash，否则 strict 模式（HUAN_STRICT_I18N=true）会报错
- i18n 构建侧（BuildMultiSite / hreflang / strict 检查 / collator / langdetect / audit）全部保留

### 文档

- ADR：`docs/adr/0015-deprecate-qwen3-translate.md`（0008 标记 Superseded）
- 报告：`docs/reports/completed/2026-08-31-deprecate-qwen3-translate.md`
- INDEX.md / CURRENT_STATE.md / MEMORY.md 同步更新

## 补充：zhurongshuo 周边同步（deploy.sh 复查 + CI + CLAUDE.md）

deploy.sh 主体改动在上一节已完成，本轮复查补齐三处遗漏：

- `deploy.sh`：preflight 警告文案去掉「翻译」（.env 只影响 R2/Worker）；step 7 注释补充「翻译已废弃，.en.md 需人工维护 source_hash」
- `.github/workflows/deploy.yml`：HUAN_STRICT_I18N 注释中 `huan translate qwen3` 提示改为「sidecar 在 huan 之外维护」
- `CLAUDE.md`：删除 `huan translate qwen3` 命令行与 `i18n/translate-prompt-zh-en.md` 条目，加废弃说明（指向 huan ADR 0015）
- `scripts/*.py`（batch_translate / translate_en / check_translation_quality）**保留**：不依赖 Ollama，是翻译 agent 的文件 I/O 辅助工具，正是废弃后的翻译工作流载体

## 补充：新版 huan 与插件安装

- 二进制：`go build -o ~/go/bin/huan ./cmd/huan` → 0.7.3 (df2072a-dirty)，含 languages.en.params 支持
- 插件：`scripts/build-plugins.sh ~/.huan/plugins` 全量重编 7 个 .so（cloudflare/diagram-renderer/html-injector/image-pipeline/seo-injector/sitemap-enhancer/zhurongshuo），与宿主同源码树（Go plugin buildid 契约，见 MEMORY.md 教训条目）
- 验证：`huan plugin list` 正常列出 5 个配置插件（无 warning）；zhurongshuo 全量构建 zh-cn=1162 / en=1109，en subTitle 注入正确
- 注意：工作树未提交（dirty），后续提交后若再装需重编同树插件
