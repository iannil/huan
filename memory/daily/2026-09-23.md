# 2026-09-23

## 脚注支持

- 用户确认“脚标”指正文数字引用、文末注释与返回链接。
- 正文原本支持 Footnote；补齐模板 markdownify 扩展与 zhurongshuo 主题样式。
- 为正文和模板补充重复引用双向链接回归检查；模板测试先复现未解析脚注，再通过修复。
- 说明与使用示例见 docs/reports/completed/2026-09-23-footnote-support.md。

## build / dev 后续性能评估

- 基于当前源码与 09-22 模板复用报告进行静态审查，未重新跑性能基准、未修改实现。
- dev 每次文件变更仍调用全量构建；watcher OnChange 不传变更集合，默认 debounce 400ms。
- 既有 IncrementalRender 仍全量加载内容、渲染 Markdown、重建上下文及聚合输出；ContentCache 在该路径是写入缓存，不是读取缓存。
- 建议优先：dev 变更分类与持久构建会话、按依赖失效、Markdown/搜索条目缓存；build 输入扫描合并、后处理减少文件往返、跨进程内容寻址缓存。
- 多语言并行需同时解决输出根目录清理、全局 i18n bundle 和插件并发契约，不能仅并行语言循环。收益需以冷/热构建及保存到浏览器更新的独立基准验证。

## build / dev 性能优化第一批落地

- 用户同意上一轮建议，完成进程内有界 Markdown LRU、dev 完整变更集合与串行重建队列、多语言输入共享读取、dev 旧目录后台清理。
- 短代码每轮仍执行，缓存键包含原正文/展开正文/Markup 配置；dev 预算 128MiB，真实站点约 95.9MiB 缓存数据。
- 实际站点交替 dev 测量：保存到 HTTP 新正文中位数 3.136s → 2.315s（-26.2%，含原有 400ms 防抖）；重建执行 2.734s → 1.912s。全量 build 2.296s → 2.264s（收益小，不能宣称显著提速）。
- 全量 go test 与 build/content/dev/cmd 的 race 检查通过；双语改名/删除/slug/正文/配置等缓存输出与干净构建一致。真实站点 7,236 稳定文件 SHA-256 一致，9 个基线既有不稳定输出没有新增差异。
- 新增 scripts/bench-build.py、scripts/bench-dev.py 和三类 Go benchmark。报告：docs/reports/completed/2026-09-23-build-dev-performance.md，机器可读数据：docs/reports/artifacts/2026-09-23-build-dev-performance.json。
- 尚未做页面级依赖失效、搜索条目缓存、跨进程缓存、多语言并行；未替换系统 huan、未发布，保留原有脚注/CSS 改动。

### build/dev 持续性能目标（进行中）

用户要求“继续优化，直到性能极致优化 /goal”，已建立持续目标。第二阶段落地搜索摘要融合、Canonify线性扫描、frontmatter内容哈希缓存、带writer lease/容量预留的持久Markdown缓存、cache watcher路径别名排除。中间实测phase1→phase2：热build2.265→1.665s、dev保存到HTTP2.268→1.648s，400ms防抖不变。7,236稳定产物一致；9个历史不稳定路径候选最终hash均由4轮旧baseline复现。详见docs/progress/2026-09-23-build-dev-performance-goal.md及artifacts。目录Mkdir、SEO惰性正文、partial Builder等后续优化正在独立验收，目标尚未完成。

### 持续性能目标验收完成（2026-09-24T00:00+08:00）

热build 2.274→1.648s，dev保存到HTTP 2.272→1.633s，重建1.870→1.231s。稳定产物/race/HTTP验收通过；缓存RSS代价及各实验取舍已记入最终验收报告。源码未提交或发布。
