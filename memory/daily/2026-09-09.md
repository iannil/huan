# 2026-09-09

## 会话：ebook-exporter 导出文件名随语言本地化

- 需求：使用 exporter 插件导出电子书时，中文版文件名用中文名（title），英文版文件名用英文名（subtitle）。
- 现状调研：
  - 文件名生成在 `plugins/ebook-exporter/plugin.go` 的 `outPath()`：`<outRoot>/<format>/<kind>/<dirName>/<base>[-en].<ext>`。
  - `baseName` 目前为 slug（individual）/ `volume-N`（volumes）/ `<kind>-complete`（complete），英文版仅追加 `-en` 后缀。
  - `BookEntry.TitleZH` 来自 `data/books.yaml` 的 `title`，`TitleEN` 来自 `subtitle`（英文书名）。
  - 聚合单元（卷/全集）的 `TitleEN` 在 `expandUnits` 中合成（"Collected Books: Volume 1" 等）。
  - manifest key `manifestKey()` 与 `baseName` 同源，用于增量跳过；修改文件名需决定 manifest key 是否随之变化。
- 澄清结论：纯标题命名；冒号转全角；`-en` 后缀默认去掉、同名冲突时保留；旧 slug 文件自动清理。
- 设计（方案 A）已获用户批准，规格已提交至 `docs/superpowers/specs/2026-09-09-ebook-export-filename-l10n-design.md`；待用户评审后进入 writing-plans。

## 会话：ebook-exporter 导出文件名本地化 — 实现计划执行完毕

- 实现计划（Tasks 1-5）已执行完毕，文件名随语言本地化功能完成开发。
- 关键提交（feature/ebook-exporter 分支）：
  - `753cddb` feat(ebook): add sanitizeFilename for localized export filenames
  - `19b1367` feat(ebook): add fileStem returning per-language title stems
  - `bf89c70` fix(ebook): empty EN title falls back to ZH title in fileStem
  - `5948349` feat(ebook): name export files by per-language book title
  - `bd4e6f9` feat(ebook): clean up stale slug artifacts after filename localization
  - 本次文档收尾提交：docs(ebook): record filename localization rollout（README 产物说明更新 + 进展文档 + 本记忆条目）
- 回归：`cd plugins/ebook-exporter && go test ./...` 与仓库根 `go build ./...` 全部通过。
- 待办：在 zhurongshuo 仓库实际执行全量导出，验收产物文件名。

## 会话：v0.11.5 发布

- 提交并推送：watcher 相对根目录修复（`5ce3e61`）+ VERSION 0.11.5（`c7a76ac`）。
- 本地安装：`go install ./cmd/huan` → `~/go/bin/huan`；8 个插件零 flag 编译 → `~/.huan/plugins/`（宿主/插件校验和一致）。
- 冒烟验证：zhurongshuo 单本导出，插件加载成功，中英文文件名本地化生效；失败仅为本地缺少 publication 字体（机器本地生成、不入库，非版本问题）。
- tag `v0.11.5` → GitHub Release（7 assets，多平台 tarball + checksums + manifest）+ GHCR 镜像 `ghcr.io/iannil/huan:v0.11.5` 均已发布。
- zhurongshuo：deploy.yml 容器 tag 升至 v0.11.5（`5b8db05fc`）。
- 遗留：`~/.huan/plugins/zhurongshuo-plugin.so` 为旧重命名残留，待用户确认后删除。
