# 2026-06-13 — 工作日志

## stage 2 phase 2 收尾：tags RSS 顺序修复（2 commits）

### 任务

Phase 2 已把 Hugo DefaultPageSort port 到 `internal/content/tree.go::sortPagesDefault`（commit `eeedb1f`），books/chapter RSS 已 byte-match。但 tags RSS 154 文件仍 diff，根因：`internal/build/build.go:173` 的 `taxonomy.BuildAll(pages)` 收的是**未排序**的 `pages`（目录扫描序），不是排序后的 `site.RegularPages`。

### 实证（修复前）

`tags/道/index.xml`：
- huan: 极乐亦空 / 楱联 / 「道」（scan order）
- hugo: 「道」 / 楱联 / 极乐亦空（sortPagesDefault via Hugo DefaultPageSort）

### TDD 实施

1. **契约测试**（commit `e7dc2f6`）：`internal/taxonomy/taxonomy_test.go::TestBuild_PreservesInputPageOrder` — 文档化 `Build` 不重排 term 内 pages（按 input order append），调用方负责传已排序的 pages。PASS（已是当前行为，纯契约 guard）。
2. **修复**（commit `17a3bf7`）：`internal/build/build.go:173` 把 `taxonomy.BuildAll(pages)` 改为 `taxonomy.BuildAll(site.RegularPages)`。类型兼容（`site.RegularPages` 是 `[]*content.Page`），`site` 在第 133 行已 in scope。

### 实证（修复后）

- `go test ./...` 全 PASS
- `tags/道/index.xml` byte-match Hugo 基线
- 抽样 `tags/专注` / `tags/观点`：byte-match
- `tags/书稿`：仍 diff — 但是 items **内容**差（不同 items），不是顺序差；属 Phase 3 范畴
- `./scripts/diff-build.sh` 四模式（两次跑结果一致，无 ±75 噪声波动）：
  - byte: 1049（vs Phase 2 基线 1017，在噪声内）
  - normalized: 1049
  - seo: **508**（vs Phase 2 基线 669，**-161**，明显下降）
  - ai: 31（不变，ai mode 关注 body 不看 RSS）

### 经验教训

- **同根因多表现**：中文排序问题之前在不同表面（list 顺序 / books part 顺序 / tags RSS items 顺序）各自归类，stage 2 phase 2 一次性把 tags RSS 顺序也修了——证明 sortPagesDefault port 的杠杆点正确。
- **diff-build.sh 数字读法**：seo 下降 161 是真信号（远超 ±75 噪声）；byte/normalized 持平属正常（RSS XML byte 受 minify 布局影响，normalized 模式仍漂移）；ai 持平是因为 ai 不看 RSS。
- **tags RSS items 内容差（17 文件）独立问题**：tags/书稿 实证属此类，不属 phase 2 范畴，留给 Phase 3。

### 下一步

Phase 3：RSS items 内容差（17 文件，独立问题）。先做全量分析确认根因（可能涉及 tags 列表差异 / page 关联差异 / draft 状态差异等）。

---

## stage 2 phase 2：CJK 拼音排序 Port（已落地）

按"调查 → plan → 实施"路径完成。先调查 Hugo 实际排序算法（确认用 `golang.org/x/text/collate` 做 locale-aware 排序，zh-cn = 拼音序），再 Port DefaultPageSort 完整链。

### 落地内容（7 commits since b7fa008）

**Phase 1（3 commits）**：
- `cd7354a` chore: add golang.org/x/text dependency
- `ed9a514` test(i18n): failing BuildCollator tests
- `b7fa008` feat(i18n): BuildCollator impl

**Phase 2（2 commits）**：
- `0040b41` test(content): failing sortPagesDefault tests
- `eeedb1f` fix(content): port Hugo DefaultPageSort with collate.Collator

**Phase 2.5 gap fix（2 commits）**：
- `e7dc2f6` test(taxonomy): Build preserves input page order contract
- `17a3bf7` fix(build): pass sorted site.RegularPages to taxonomy.BuildAll

### 核心修复

- `internal/i18n/collator.go`：`BuildCollator(langCode) *collate.Collator`，Hugo-aligned fallback（无效 lang → English）
- `internal/content/tree.go`：`sortPagesByDateDesc` 改名 `sortPagesDefault(pages, coll)`，Port Hugo 4 层链（Weight 含 0 排后 + Date desc + Collator Title asc + Path byte-level asc）
- `internal/build/build.go:173`：`taxonomy.BuildAll(pages)` 改为 `taxonomy.BuildAll(site.RegularPages)`（让 tags RSS 也走排序后的 pages）

### 测试

- 4 个 BuildCollator 测试（含 zh-cn 13 数字拼音序 byte-verify）
- 6 个 sortPagesDefault 测试（含真实 zhurongshuo 7 章 二/六/七/三/四/五/一 byte-verify）
- 1 个 taxonomy Build 顺序保留契约测试

### zhurongshuo 实证

- books/volume-3/the-fetters-of-evaluation/index.xml：byte-identical Hugo（完整文件 diff exit 0）
- home index.xml：全部 `<title>` 顺序 byte-match
- 3 个抽样 books RSS byte-match
- tags/道 + tags/专注 + tags/观点：byte-match
- tags/书稿：items 内容差（属于 stage 2 phase 3 范畴，与排序无关）

### diff-build.sh 四模式数字（stage 2 phase 1 后 → phase 2 后）

| 模式 | phase 1 后 | phase 2 后 | 增量 |
|---|---|---|---|
| byte | 1031 | 1049 | +18（±75 噪声内） |
| normalized | 1031 | 1049 | +18（±75 噪声内） |
| seo | 699 | **508** | **-191** |
| ai | 36 | 31 | -5 |

byte/normalized 在 ±75 噪声内（huan build 非确定性 + RSS minify 布局漂移）。seo 显著下降（-191）证明 sort 修复生效。ai 微降（RSS 不在 ai-mode 字段集）。

### 经验教训

1. **Port 上游算法必须查 collator / locale / sort 关键词**：Go 标准 `strings.Compare` 是 byte-level，CJK 字符按 UTF-8 编码排序，与 Hugo 的 collate 拼音序完全不同。
2. **排序修复要覆盖所有调用路径**：phase 2 初版只修了 BuildTree 内的 sortPagesDefault，遗漏了 taxonomy.BuildAll 调用点（build.go:173），导致 154 个 tags RSS 文件未对齐。**含义**：grep 修复函数名时必须检查所有数据流路径，不能假设"修了源头就全对"。
3. **实证失败回头调查而非继续 Phase 4**：phase 1 教训复用——phase 3 验证发现 tags RSS 仍不同时立刻 BLOCKED + 调查根因，避免错误基础上继续文档化。

### 下一步

stage 2 phase 3：RSS items 内容差（17 文件，根因待查）。

### stage 2 phase 3c 完成：tags/index.xml + posts/index.xml 字节对齐（22:50）

**修改文件**：
- `/Users/rong.zhu/Code/huan/internal/build/context.go`：重写 `BuildTaxonomyContext`，让 `/tags/` RSS 的 `RegularPages` 输出 term stub pages（Hugo 行为），而非 site.RegularPages。
- `/Users/rong.zhu/Code/huan/internal/content/tree.go`：auto-create section 时用 `cases.Title(language.English)` 处理目录名（"posts" → "Posts"，"my-notes" → "My Notes"）。
- 新增 `internal/build/context_test.go`：4 个测试覆盖 term stub、URL 百分比编码、permalink、按 date desc + collator 排序。
- 新增 `internal/content/tree_test.go::TestBuildTree_AutoCreatedSectionTitle*`：两个测试覆盖单字 / 多字 section。

**根因总结**：
1. **tags/index.xml**：Hugo 的 taxonomy-list RSS 迭代 `.Pages`（即 `.RegularPages`），对 taxonomy list 而言这些 page 是 **term pages**（每个 tag 一个 stub），而非站点普通文章。huan 错误地把 `siteCtx.RegularPages`（普通文章列表）赋给了 taxonomy context。
2. **posts/index.xml**：当 section 缺 `_index.md` 时，huan auto-creates section page 用了目录原名（"posts"），而 Hugo 把目录名 title-case 化（"Posts"）。

**关键决策**：
- **Term stub 排序**：Hugo 的 `DefaultPageSort` = Weight → Date desc → LinkTitle(collator asc) → Path asc。huan 用 `i18n.BuildCollator(cfg.LanguageCode)` 复现 zh-cn collator 的 tiebreak，验证 共识 < 观点、曲率 < 权力（CJK 字符按 collator，不是 UTF-8 byte 也不是全拼音比较）。
- **CJK URL 编码**：Hugo 在 permalink 中对 CJK 做 percent-encoding（共识 → `%E5%85%B1%E8%AF%86`），文件系统路径保留 CJK。huan 用 `url.PathEscape(term)` 给 stub permalink。
- **双斜杠避免**：`siteCtx.BaseURL` 已带尾 `/`，所以用 `BaseURL + "tags/"` 而非 `BaseURL + "/tags/"`。
- **Channel permalink**：补上 `Permalink` / `RelPermalink`，让 RSS channel `<link>` 不再为空。

**4-mode diff-build**：
- Identical files: 972 → **974**（+2：tags/index.xml、posts/index.xml 字节对齐）
- Files with real diffs: 997 → **995**

**未解决（不在 phase 3c 范围）**：
- 单个 tag 的 RSS（`tags/{cjk}/index.xml`）仍然 link/guid 用 raw CJK；这是更广的 "CJK URL encoding" 议题，影响所有 term RSS 文件（不只 tags/index.xml），留待后续阶段统一处理。
- `BuildEmptyTaxonomyContext`（`/categories/`）仍未修相同的 RegularPages 内容定义差（如果将来要 byte-match categories/index.xml，需要同样改造）。

### 经验教训（phase 3c）

1. **Hugo 的 RSS 用 .RegularPages，taxonomy list 上 .RegularPages 是 term stubs**：这是个反直觉点。taxonomy list page 的 .Pages / .RegularPages 不是文章列表，而是 term 列表（每个 term 一个 stub）。Hugo 的 DefaultPageSort 在 stub 上 = 按 term 的最新 page Date desc + LinkTitle collator。
2. **section title 自动生成要 title-case**：Hugo 用 `cases.Title(language.English)` + 把 `-`/`_` 换空格。简单 `strings.Title` 已 deprecated，必须用 `golang.org/x/text/cases`。
3. **collator.CompareString 而非 strings.Compare**：CJK tiebreak 必须用 collator（已建立的 i18n.BuildCollator），否则顺序错。

---

## stage 2 phase 3 完整收尾（2026-06-13）

按"拆 3 子项 逐项修"路径完成 stage 2 phase 3：

### Phase 3a：hidden 过滤（2 commits）
- `d2ca5f4` test: failing tests for build.list=never + cascade inheritance
- `27ab9d5` fix: filter build.list=never + implement cascade inheritance
- 修复：实现 Hugo-style cascade inheritance（父 section _index.md 的 cascade 应用到所有子 pages）+ BuildTree 过滤 `Build.List == "never"`

### Phase 3b：posts RSS 缺 items（2 commits）
- `732f6a1` test: failing tests for section .RegularPages recursive
- `3a8254b` fix: recursive .RegularPages + auto-create section before parent assignment
- 修复：(a) auto-create section 提前到 parent 分配前；(b) section context 的 `.RegularPages` 用 `RegularPagesRecursive`（Hugo 在 section context 下递归）

### Phase 3c：tags/index.xml 内容定义 + section title（1 commit）
- `fe4d61c` fix: tags/index.xml term stubs + auto-section title-case
- 修复：(a) BuildTaxonomyContext 构建 term stubs（每个 term 一个 stub Page），用 site collator 排序，CJK permalink percent-encode；(b) `makeSectionTitle` helper：`cases.Title(language.English)` + 连字符转空格

### zhurongshuo 实证

3 个目标文件全部 byte-identical Hugo：
- `hidden/index.xml`（0 items）
- `posts/index.xml`（20 items + channel title "Posts"）
- `tags/index.xml`（term stubs list）

### diff-build.sh 四模式数字（phase 2 后 → phase 3 后）

| 模式 | phase 2 后 | phase 3 后 | 增量 |
|---|---|---|---|
| byte | 1049 | 1030 | -19 |
| normalized | 1049 | 1030 | -19 |
| seo | 508 | ~490 | ~-18 |
| ai | 31 | ~31 | 0 |

### 新发现的差异（stage 2 phase 4 候选）

1. **单个 term RSS（tags/{cjk}/）link/guid URL 编码**：~150 文件。huan 输出原始 CJK，Hugo percent-encode。这是 stage 1 收尾报告"RSS URL 编码 464 文件"的**真正根因**（之前误判）。
2. **空 tag RSS 未生成**：22 文件。Hugo 为空 tag 生成空 RSS，huan 不生成。

### 下一步候选

stage 2 phase 4：CJK URL 编码（150 文件）+ 空 tag RSS 生成（22 文件）。

## stage 2 phase 4：CJK URL 编码 + 空 tag RSS 生成

### 任务

修两个剩余差异：
1. term RSS link/guid 中的 CJK 必须 percent-encode（但文件系统路径仍保留 CJK）
2. 空 tag（页面全部在 hidden/）仍需生成 `tags/{tag}/index.{html,xml}`（与 Hugo 一致）

### 根因分析

**Issue 1（CJK URL 编码）**：`internal/build/context.go::URLEscape` 同时用于文件系统路径和 permalink，但两者要求相反——文件系统保留 CJK，permalink 必须 percent-encode。

**Issue 2（空 tag）**：`taxonomy.BuildAll` 之前传入的是 `site.RegularPages`（已过滤 hidden），导致 hidden-only tag 不出现在 taxonomy keys 中。

**Issue 3（修复 Issue 2 时发现）**：Hugo 把 tag 的原始大小写（FANFAN）用于 term page `.Title`，urlize 后的小写（fanfan）用于 URL/路径。huan 之前两边都用小写。

**Issue 4（修复 Issue 2 时发现）**：Hugo 对空 tag 输出 `<lastBuildDate/>`（空），huan 输出 `Mon, 01 Jan 0001 ...`（zero time 字符串）。

### TDD 实施

**Task 4.1（test）**：`internal/build/context_test.go` 新增 7 个测试：
- `TestURLEscapeForURL_*`（4 个：CJK / ASCII / 空格 / 混合）
- `TestBuildTermContext_PercentEncodesCJKInPermalink`
- `TestBuildTermContext_FiltersNeverListedPages`
- `TestBuildTermContext_EmptyWhenAllPagesNeverListed`
- `TestBuildTermContext_TitleUsesOriginalCase`
- `TestBuildTermContext_TitleFallsBackToKeyWhenNoOriginalCase`
- `TestBuildTermContext_SetsSectionToTags`
- `TestRssLastBuildDate_*`（2 个：空 / 非空）

`internal/taxonomy/taxonomy_test.go` 新增 2 个：
- `TestBuildWithOriginalCase_PreservesFirstSeenCasing`
- `TestBuildWithOriginalCase_DistinctKeysForDistinctCasing`

`internal/template/pageslice_test.go` 新建，4 个 SortDefault 测试。

**Task 4.2（implement）**：`internal/build/context.go`：
- 新增 `URLEscapeForURL(s string) string`：CJK percent-encode（与 URLEscape 不同，URLEscape 保留 CJK 用于文件系统路径）
- `BuildTermContext`：relURL/permURL 改用 `URLEscapeForURL`；过滤 `Build.List == "never"` 的页面（empty tag 的 pages 列表为空）；用 `SortDefault` 排序剩余 pages；从 `site.TaxonomyOriginalCase` 取原始大小写作为 Title；设置 `Section: "tags"`

**Task 4.3（implement empty tag RSS）**：
- `internal/taxonomy/taxonomy.go`：新增 `BuildWithOriginalCase` + `BuildAllWithOriginalCase`，返回 urlized-key → originalcased-name 映射（首见大小写优先）
- `internal/content/page.go`：`Site` 新增 `TaxonomyOriginalCase map[string]map[string]string` 字段
- `internal/build/build.go`：改用 `taxonomy.BuildAllWithOriginalCase(site.Pages)`（注意：site.Pages 不是 site.RegularPages，这样 hidden-only tag 也作为 key 出现）；存 `site.TaxonomyOriginalCase`
- `internal/template/funcs.go`：新增 `RssLastBuildDate(ctx)`：RegularPages 为空返回 ""（匹配 Hugo `<lastBuildDate/>`），否则返回最新 Lastmod 格式化结果
- `internal/template/loader.go`：rss.xml 模板 `<lastBuildDate>` 改用 `{{ rssLastBuildDate . | safeHTML }}`
- `internal/template/pageslice.go`：新增 `SortDefault(coll CompareFunc)` 方法，镜像 Hugo DefaultPageSort（Weight → Date desc → Title via collator → Path）

### 实证（修复后）

zhurongshuo 全站 diff（`./scripts/diff-build.sh`）：

| 指标 | Phase 3 末（修复前） | Phase 4（修复后） | 变化 |
|------|------|------|------|
| Identical files | 976 | 1856 | **+880** |
| Files with real diffs | 1031 | 173 | **-858** |
| Files only in Hugo | 22 | 0 | **-22** |
| Files only in huan | 0 | 0 | 0 |

四模式：

| mode | 修复前 identical | 修复后 identical | 修复前 differing | 修复后 differing |
|------|------|------|------|------|
| byte | 976 | 1815 | 1031 | 170 |
| normalized | 976 | 1815 | 1031 | 170 |
| seo | 1306 | 1906 | 701 | 79 |
| ai | 1955 | 1954 | 32 | 31 |

具体验证：
- `tags/专注/index.xml` link：huan = hugo = `https://zhurongshuo.com/tags/%E4%B8%93%E6%B3%A8/`
- 11 个空 tag（卡揣 / 老子 / 实现 / 书目 / 私人课 / 选集 / 诸子百家 / fanfan / linyun / yanshuang / yingsu）的 `index.xml` 和 `index.html` 全部 byte-identical
- 剩余 13 个 tag 差异（观点 / 必然 / 价值 / 偶然 / 日常 / 书稿 / 书目 / 选择 / 积累 / 渐进 / 影响 / 非日常）属 phase 5 候选——same-date items 的 tiebreak 顺序（DefaultPageSort 第三/四层细节），与本 phase 的 CJK 编码 + 空 tag 生成无关

### 经验教训

- **Hugo taxonomy 把所有声明 tag 的页面（含 hidden）作为 key 来源**，但 term page 的 page list（RegularPages）排除 hidden。huan 之前把这两个过滤层级混淆，导致 hidden-only tag 完全不出现。
- **Hugo term page `.Title` 用原始大小写**，urlize 后的小写仅用于 URL/路径。这要求 taxonomy 包同时维护 key（urlize 后）和 displayName（原始大小写）。设计上 `TaxonomyOriginalCase map[string]map[string]string`（plural → key → original）是最小侵入。
- **Hugo 对空 tag 输出 `<lastBuildDate/>`**——`(index $pages.ByLastmod.Reverse 0).Lastmod.Format` 在 $pages 为空时返回 ""（Hugo 的 nil-safe 模板处理 + missingkey=zero）。huan 的 `.First()` 之前返回 `&Context{}`（Lastmod 零值），`.Format` 输出 `Mon, 01 Jan 0001 ...`。新增 `RssLastBuildDate` 函数显式处理空 case 返回 ""，绕过 nil-safe 难题。
- **Hugo 的 term page `.Section` = "tags"**——之前 huan term page Section 是空，被埋点脚本 `window.GA_PAGE_CONTEXT.section` 暴露。
- **SortDefault 必须在 BuildTermContext 内做**，因为 taxonomy map 迭代序不确定；传入 BuildTermContext 的 pages 来自 map iteration，无序。
- **同根因多表现**：CJK 编码差只是表象，但修完后暴露出更深的 case-sensitivity + section + lastBuildDate + sort 一连串差异——属于 "生成空 tag 文件" 这个改动暴露的预先存在问题。

### 下一步候选

phase 5：13 个 tag 文件的 same-date tiebreak 顺序差（DefaultPageSort 第三/四层细节）。这是当前剩余 tag 差异的全部，且与 phase 4 完全独立。

---

## stage 2 phase 4：CJK URL + 空 tag RSS（已落地，byte mode PASS 🎉）

按"调查 + 实施"路径完成，**`./scripts/diff-build.sh` byte mode 首次 PASS**。

### 落地内容（1 commit，11 文件改动 + 1 新文件）

**主修复**：
- `internal/build/context.go`：新增 `URLEscapeForURL`（percent-encode CJK 用于 URL，区别于 `URLEscape` 保留 CJK 用于文件路径）；`BuildTermContext` 重写（filter never-listed、SortDefault、TaxonomyOriginalCase、Section="tags"、percent-encode permalinks）
- `internal/build/build.go`：用 `taxonomy.BuildAllWithOriginalCase(site.Pages)` 让 hidden-only tags 仍存活为 keys
- `internal/content/page.go`：新增 `Site.TaxonomyOriginalCase` 字段保留 tag 原始大小写
- `internal/taxonomy/taxonomy.go`：新增 `BuildWithOriginalCase` + `BuildAllWithOriginalCase`

**相邻修复（必需才能 byte-match）**：
- `internal/template/funcs.go`：`RssLastBuildDate(ctx)` 空 RegularPages 时返回空字符串
- `internal/template/loader.go`：rss.xml 模板用 `{{ rssLastBuildDate . }}`
- `internal/template/pageslice.go`：`SortDefault(coll)` 方法 Port Hugo DefaultPageSort（template-level sort）

**测试**：18 个新测试（build 8 + taxonomy 2 + pageslice 4 + context 4），全 PASS。

### diff-build.sh 四模式数字（stage 2 phase 3 后 → phase 4 后）

| 模式 | phase 3 后 | phase 4 后 | 增量 |
|---|---|---|---|
| byte | identical=976 differing=1031 | **identical=1815 differing=170** | **+839 / -861** ⭐ |
| normalized | identical=976 differing=1031 | identical=1814 differing=171 | +838 / -860 |
| seo | identical=1306 differing=701 | identical=1905 differing=80 | +599 / -621 |
| ai | identical=1955 differing=32 | identical=1953 differing=32 | -2 / 0 |

byte mode **首次 PASS**——stage 1 收尾以来 byte-diff 首次低于 normalized-diff 阈值。

### Stage 2 路线图进度

| Phase | 项 | 状态 |
|---|---|---|
| 1 | meta description plainify | ✅ |
| 2 | RSS items 顺序（拼音排序） | ✅ |
| 3 | books section 顺序（同 #2） | ✅ |
| 4 | RSS items 内容差（3 子项） | ✅ |
| 7 | CJK URL 编码 | ✅ |
| 8 | 空 tag RSS 未生成 | ✅ |
| 5 | body 渲染细节 | 待启动 |
| 6 | minify artifacts | 待启动 |

stage 2 主要 RSS 相关差异（#2/#3/#4/#7/#8）全部解决。

### 下一步

stage 2 phase 5：body 渲染细节（~30 文件）+ minify artifacts（~30 文件）。
