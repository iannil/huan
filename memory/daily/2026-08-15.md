# 2026-08-15

## 开发进度梳理（会话开始）

- 用户要求梳理当前开发进度；本次为只读盘点，未改代码。
- 盘点结论（要点）：
  - 版本 v0.7.1（2026-07-30 `4ad0536`），master 干净，`go build ./...` 通过。
  - 最近一条完整 epic：`--plugins` flag（build/dev 覆盖插件目录）已实现并提交（`cb12eaa`/`5b7ea1c`/`ef4d227`/`841b12e`），但 `docs/superpowers/plans/2026-07-30-plugins-flag.md` 的 checkbox 全部未勾选（Task 4 原定的测试更新改为新增回归测试 `841b12e`）。
  - 7-28~7-30 密集完成：插件契约收敛（ADR 0013）、Hook 契约拆分修复 .so 钩子静默失效（ADR 0014）、diagram-renderer 插件、release.sh 一键发布、huan.example.yaml。
  - 文档滞后：CURRENT_STATE.md 停在 2026-07-25（写 v0.6.0+，实际 v0.7.1）；daily 笔记停在 07-28（缺 07-29/07-30 两天）；MEMORY.md 停在 07-28。
  - v1.0 gate 6（zhurongshuo 生产稳定 90 天）：2026-06-13 起算，至今天 63 天，窗口 2026-09-11 到期。

## 文档补账（完成）

- 按用户指示补齐滞后文档，5 项全部完成（未提交，工作树留待用户确认）：
  1. 补写 `memory/daily/2026-07-29.md`（diagram-renderer epic + 两个 bug 修复，标注"补记"）
  2. 补写 `memory/daily/2026-07-30.md`（release.sh + --plugins flag + 0.7.1，标注"补记"）
  3. `MEMORY.md`：版本至 v0.7.1；新增 07-29/30 上下文与 3 条经验；**更正** `$HUAN_HOME` 决策为 `$HUAN_HOME/plugins`（07-29 `8d06b30` 已改代码但记忆未同步）
  4. `CURRENT_STATE.md`：v0.6.0+→v0.7.1、演进表加 v0.7 行、新增 3.11~3.13 节、结构图补 pkg/ 与 8 插件、gate 6 进度 63 天
  5. 回勾两个 plan checkbox（plugins-flag 17 项、diagram-renderer 39 项全勾），plan 头部加完成状态注记，plugins-flag Task 4 偏差已注明（新增回归测试替代更新旧测试）
- 核实手段：git log 考古 + 代码逐项对照（`newPluginRegistry` 签名、`--plugins` flag 两处、renderer fence-lang 修复、build-plugins.sh 门控）+ `go test ./plugins/diagram-renderer/ ./internal/markdown/ ./cmd/huan/` 全绿。
- 发现并修正的记忆偏差：MEMORY.md 中"插件查找顺序 `$HUAN_HOME`→项目"已被 07-29 提交更正为 `$HUAN_HOME/plugins`，旧记录未跟上。

## E2E 测试用例体系设计（brainstorming 完成，未实现）

- 用户要求把通用 E2E 测试方案（浏览器 E2E + API 集成、结构化 YAML、自包含 setup/teardown）改造为适合 huan 的体系。
- 澄清结论：Agent 驱动执行（不写 runner/测试代码）；全功能分层覆盖；fixture 站点 + 临时目录；文件系统即数据库；放 tests/e2e/；主用途开发验收 + 回归（不接 CI）。
- 选定方案 B：分层双 schema——api/cli 用严格结构化 schema（curl 可复现），browser 用旅程式 schema（意图 + 里程碑，选择器只是 hints）。
- 规模：10 个 API 套件 ~78 用例 + 5 个 browser 文件 ~22 旅程 + 4 个 CLI 文件 15 用例；3 个 fixture（minimal/multilang/with-plugins）+ runbooks（RUNBOOK.md + patterns.md）。
- Spec 已提交：`docs/superpowers/specs/2026-08-15-e2e-test-system-design.md`（`f539456` + 自审修正 `7ba2334`）。
- 关键设计点：两条硬规则（每个 http expect 必须显式断言 status+关键字段；每个写操作后必跟 fs.assert 落盘断言）；`${token}` 从启动 stderr 捕获；历史 bug 回归项（draft 泄露公开 API、page=MaxInt64 溢出、q 超 200 rune）编入用例。
- 下一步：writing-plans → 实现计划。

## E2E 实现计划完成（writing-plans）

- 计划已提交：`docs/superpowers/plans/2026-08-15-e2e-test-system.md`（`e8fdad2`），13 个 Task。
- 写计划前做了系统性**探针实测**（编译二进制 + 搭最小站点 + 起 dev/daemon），关键发现：
  1. **daemon admin token 鉴权缺陷（真实 bug）**：`internal/daemon/daemon.go:215` 硬编码 `Token: ""`，注释声称 "Uses env var HUAN_ADMIN_TOKEN" 但 `TokenMiddleware` 无 env 回退——实测设了 env 的正确请求也 401。dev 模式流转正确（dev.go:53-62 有完整 ResolveToken/CheckBindSafety/GenerateToken 链）。已列为计划 Task 1（修复 + 回归测试）。
  2. dev 模式不挂载 /health、/api/v1/*、SSE（这些只在 daemon）——用例 runtime 分配据此修正：admin API 套件用 dev，公开 API/SSE 套件用 daemon。
  3. 无 layouts 时 build 渲染 0 页 Errors:1；fixture 必须带最小 layouts（single.html + list.html + index.html）；list 模板用 `.RelPermalink`（`.URL` 在 list 上下文不可求值）。
  4. token 捕获契约：stderr `grep -oE '[0-9a-f]{32}'`。
- 计划要点：Task 1 修 daemon token → Task 2-3 三 fixture（FIXTURES.md 已知状态表，实测校准）→ Task 4 runbook+patterns → Task 5-8 十个 API 套件 78 例 → Task 9-11 browser schema+5 文件 22 旅程 → Task 12 CLI 15 例+排除清单 → Task 13 全量验证+基线报告。
- 下一步：用户选执行方式（subagent-driven 或 inline）。

- E2E 测试体系 Task 1（前置修复）：daemon 模式 admin token 鉴权失效修复完成。
  - `internal/daemon/daemon.go` 新增 `resolveAdminToken`（env 优先 / loopback 自动生成 / 非 loopback 无 env 拒启，与 dev.go 流程一致），`NewHandler` 的 `Token` 由硬编码 `""` 改为解析结果。
  - 新建 `internal/daemon/token_test.go`：`TestResolveAdminToken` 三子测试 + `writeFile` helper。
  - 提交 `e0a8f2e`；daemon 包 52 测试全绿；冒烟确认 `HUAN_ADMIN_TOKEN=fixedtok` 时 `/admin/api/status` 返回 status JSON（修复前 401）。
  - 详见 `.superpowers/sdd/2026-08-15-e2e-test-system/task-1-report.md`。

- E2E 测试体系 Task 7（settings/build-trigger/public-api 三套件 YAML）完成。
  - 交付 `tests/e2e/api/{settings,build-trigger,public-api}.yaml`（9+7+12=28 用例），提交 `af0d7f2`。
  - 关键实测校准：dev/daemon 构建产物在进程私有 MkdirTemp 目录（不写 $site/public），产物断言一律走 HTTP 层；PUT settings 是全量覆盖（零值会清掉未传字段）；pub-005 page=MaxInt64 实测 clamp 到 100000 回显；dev 无 JIT（JIT 在 daemon serving.go），bld-006 改 --disableWatch+POST build 口径。
  - bld-004 按 FIXTURES.md 偏差 1 收窄：只断言 draft 单页 404 + 公开 API 排除，不断言聚合（已知引擎缺陷）。
  - 抽样实跑 6/6 PASS（set-001/002、bld-001/004、pub-001/009）；YAML 硬规则 1/2 机器校验三套件全过。
  - 详见 `.superpowers/sdd/2026-08-15-e2e-test-system/task-7-report.md`。

- E2E 测试体系 Task 13（全量验证与文档收尾）完成——体系首次全量实跑。
  - 全量 YAML 校验：19 个 YAML（10 api + 5 browser + 4 cli）python3 safe_load 全过。
  - 实跑（run_id 20260815-122309，二进制 215569d 现场 build，RUNBOOK 端口表 + HUAN_HOME 隔离 + 固定 token）：
    - API 10 套件：P0 全跑 + 其余抽 2，65/65 全过（auth 6、status 4、content-crud 8、media 5、settings 8、build-trigger 5、public-api 12、sse-events 4、livereload 4、plugins-admin 9）。零 YAML 修正——前序两轮预校准已消化全部偏差。
    - CLI 4 套件 15/15 复跑全过（cb 8、cn 3、cc 3、cv 1）。
    - browser：agent-browser 实走 10 旅程（5 个 P0：lg-001/bc-001/bc-002/sr-001/sr-002 + lg-002/lg-003/bc-003/bc-004/sr-005）全过，12 张截图入 docs/reports/e2e/assets/20260815-122309/。
  - FIXTURES.md with-plugins 偏差 4 回改：total=1（Task 3 旧口径）→ 双 .so 编译后 total=2、simple-plugin.so 加载后 total=3（Task 8 + Task 13 两次复测，注明更新日期与来源）。
  - 基线报告：docs/reports/e2e/2026-08-15-system-baseline.md（环境/执行范围/每套件结果表/7 项 bug 清单终态/与 FIXTURES 引用关系）。
  - 文档收尾：CURRENT_STATE.md 追加「十、E2E 测试体系」小节；MEMORY.md 项目上下文追加 E2E 契约段。
  - teardown：pgrep 无残留；/tmp 站点与 artifacts 已清理（截图已拷出）。

## E2E-found 引擎 bug 修复（SDD 执行完成，6 Task 全过）

- 计划 `docs/superpowers/plans/2026-08-15-fix-e2e-found-bugs.md` 在 worktree `fix-e2e-bugs` 全量执行：
  - T1 draft 聚合：PopulateSitePages + LinkPageRelationships 都加 includeDrafts（修复中发现 RSS 走 LinkPageRelationships 的 ctx.RegularPages 绕过 site 级过滤，补第 2 个 commit）→ sitemap/RSS/首页/列表/tag 全零泄露、-D 保留
  - T2 en 删除：前端 11 处 relPath→filePath + 后端 remove 加语言变体防御（报真实文件名不猜删）+ 新旅程 bc-007
  - T3 Settings：hasChanges useRef→useState（7 处）
  - T4 dev 插件面：dev.go 复用 reg 构造 LifecycleManager 传 admin + listPlugins nil 改 503（2 个既有测试断言 200 同步改 503）
  - T5 daemon build 接 PluginRegistry：7.5 块前移 + BuilderOptions 加字段 + 透传全量/增量/JIT 三路径 → daemon serve 页恢复注入（og:×4 实测）
  - T6 回归 + 4c 定性更正：全量 32 包绿、22 YAML ok、bld-004 sitemap 零 draft 抽样过；FIXTURES 毒化表述改为「跨树 buildid 失配环境契约」
- 子代理多次被 403 拒（token 无 sonnet/haiku 模型权），Task 1/3/4/5 由控制器直接实现+审查，Task 2 子代理完成。审查以控制器源码核验替代（subagent review 两度 403）。

## 全量逐例实跑（E2E 117 例 + Go 32 包）

- 用户要求「全量通过」，做了一次**真正逐例全跑**（非抽样）：
  - CLI 15 全过、API 79 全过（auth/status/crud/media/settings/build-trigger/public-api/sse/plugins/livereload）
  - browser 23 旅程 agent-browser 实走：22 PASS + 1 PARTIAL（bs-002）
  - Go `go test ./...` 32 包全绿
- **新增 3 个发现（抽样基线未见）**：
  1. **bs-002 PARTIAL**：Settings 改标题保存后 Dashboard 顶栏显示旧站名——`status.title` 是 handler 静态副本（api.go:38 siteTitle），settings PUT 的 async rebuild 不刷新（P2 真 bug）
  2. **多语言编辑器顶栏 `.zh-CN.zh-CN.md` 双后缀显示**：ContentEdit 直渲染 detail.filePath，realFilePath 在 relPath 已含语言后缀时重复叠加（P2 显示 bug，保存走 relPath 安全）
  3. 内嵌 SPA bundle 时效：E2E 前需重建 web/admin dist（bc-007 初测踩旧 bundle 500）
- 报告：docs/reports/e2e/2026-08-15-full-run.md（115/117 PASS 全量基线，与抽样基线的 90/90 对照）
