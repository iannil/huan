# 2026-08-15

- E2E 测试体系 Task 1（前置修复）：daemon 模式 admin token 鉴权失效修复完成。
  - `internal/daemon/daemon.go` 新增 `resolveAdminToken`（env 优先 / loopback 自动生成 / 非 loopback 无 env 拒启，与 dev.go 流程一致），`NewHandler` 的 `Token` 由硬编码 `""` 改为解析结果。
  - 新建 `internal/daemon/token_test.go`：`TestResolveAdminToken` 三子测试 + `writeFile` helper。
  - 提交 `e0a8f2e`；daemon 包 52 测试全绿；冒烟确认 `HUAN_ADMIN_TOKEN=fixedtok` 时 `/admin/api/status` 返回 status JSON（修复前 401）。
  - 详见 `.superpowers/sdd/2026-08-15-e2e-test-system/task-1-report.md`。

- E2E 测试体系 Task 7（settings/build-trigger/public-api 三套件 YAML）完成。
  - 交付 `tests/e2e/api/{settings,build-trigger,public-api}.yaml`（9+7+12=28 用例），提交 `af0d7f2`。
  - 关键实测校准：dev/daemon 构建产物在进程私有 MkdirTemp 目录（不写 $site/public），产物断言一律走 HTTP 层；PUT settings 是全量覆盖（零值会清掉未传字段）；pub-005 page=MaxInt64 实测 clamp 到 100000 回显；dev 无 JIT（JIT 在 daemon serving.go），bld-006 改 --disableWatch+POST build 口径。
  - bld-004 按 FIXTURES.md 偏差 1 收窄：只断言 draft 单页 404 + 公开 API 排除，不断言聚合（已知引擎缺陷）。
  - 抽样实跑 6/6 PASS（set-001/002、bld-001/004、pub-001/009）；YAML 硬规则 1/2 机器校验三套件全过。
  - 详见 `.superpowers/sdd/2026-08-15-e2e-test-system/task-7-report.md`。

- E2E 测试体系 Task 13（全量验证与文档收尾）完成——体系首次全量实跑。
  - 全量 YAML 校验：19 个 YAML（10 api + 5 browser + 4 cli）python3 safe_load 全过。
  - 实跑（run_id 20260815-122309，二进制 215569d 现场 build，RUNBOOK 端口表 + HUAN_HOME 隔离 + 固定 token）：
    - API 10 套件：P0 全跑 + 其余抽 2，65/65 全过（auth 6、status 4、content-crud 8、media 5、settings 8、build-trigger 5、public-api 12、sse-events 4、livereload 4、plugins-admin 9）。零 YAML 修正——前序两轮预校准已消化全部偏差。
    - CLI 4 套件 15/15 复跑全过（cb 8、cn 3、cc 3、cv 1）。
    - browser：agent-browser 实走 10 旅程（5 个 P0：lg-001/bc-001/bc-002/sr-001/sr-002 + lg-002/lg-003/bc-003/bc-004/sr-005）全过，12 张截图入 docs/reports/e2e/assets/20260815-122309/。
  - FIXTURES.md with-plugins 偏差 4 回改：total=1（Task 3 旧口径）→ 双 .so 编译后 total=2、simple-plugin.so 加载后 total=3（Task 8 + Task 13 两次复测，注明更新日期与来源）。
  - 基线报告：docs/reports/e2e/2026-08-15-system-baseline.md（环境/执行范围/每套件结果表/7 项 bug 清单终态/与 FIXTURES 引用关系）。
  - 文档收尾：CURRENT_STATE.md 追加「十、E2E 测试体系」小节；MEMORY.md 项目上下文追加 E2E 契约段。
  - teardown：pgrep 无残留；/tmp 站点与 artifacts 已清理（截图已拷出）。
