# 2026-07-30

> **补记**：当日未写笔记，本篇于 2026-08-15 依据 git 历史重建。

## release.sh 一键发布脚本

- `91d2dc8`：`scripts/release.sh`——huan 二进制 + 全部插件 .so + checksums.txt + manifest.json 一条命令出齐。
- 设计决策：Shell 脚本（非 Go 子命令，避免鸡生蛋）；产物在 `release/v{version}/{os}-{arch}/`；不删旧产物；插件编译失败跳过并汇总。
- `3ae103f`：文档三件套（`docs/progress/release-script.md` + plan + design）。
- 实测：v0.7.0 产物完整（二进制 + 8 插件），checksums 9 文件全过。

## `--plugins` flag（build/dev 覆盖插件目录）

- 设计与计划先行：`5c28bbb`（spec + plan，4 task）。
- Task1 `cb12eaa`：`newPluginRegistry` 加 `pluginDirOverride` 参数（非空则替换默认 `sourceDir/plugins/`）；连带更新 8 个调用点文件。
- Task2 `5b7ea1c`：`huan build --plugins`。
- Task3 `ef4d227` + `ab1fd50`：`huan dev --plugins`（初始构建 + rebuild 闭包两处）+ gofmt。
- Task4 偏差执行 `841b12e`：原计划是更新两个旧测试的签名调用；实际旧测试已随 Task1 一并更新，改为**新增**回归测试 `TestNewPluginRegistry_PluginDirOverride`（覆盖 override 指向无 .so 目录的场景）。
- 语义边界（设计约束）：`$HUAN_HOME/plugins` 全局目录仍优先于 `--plugins`；空值 = 默认行为；`plugin list/info` 子命令不受影响。

## 版本

- `4ad0536`：VERSION → **0.7.1**（v0.7.0 tag 打于 07-28 21:44 `c6e7f11`）。

## 文档欠账（已知，待后续补）

- 当日未写 daily 笔记；CURRENT_STATE.md 未同步至 v0.7.x；两个 plan checkbox 未回勾。——已于 2026-08-15 补齐。
